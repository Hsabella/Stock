# 红绿灯体系使用手册

> 日常操作只需要这一份文档。术语查 [GLOSSARY.md](./GLOSSARY.md)，
> 回测依据查 `docs/quant/`（主灯 phase1_report / 结构灯 timing_lights_report / 个股抄底灯 stock_lights_report）。
> 旧引擎（8 维打分）的说明在 [USAGE.md](./USAGE.md)——其 BUY 名单已证伪，只有排雷信号还在为晨报供数。

## 30 秒理解这套系统

**它不选股**（两次选股尝试均被回测证伪），它只回答三件事：

1. **今天该拿多少总仓位**（主灯 + 结构灯 = 账户风险预算）
2. **手里的票有没有危险**（止损线 + 引擎排雷）
3. **watchlist 里的票什么时候是好买点**（个股抄底灯 = 单笔时机）

买什么票（watchlist 名单）由你定；系统管"何时、多少、何时撤"。

---

## 每天 1 分钟：读晨报

工作日 09:00 自动生成 **`results/brief/latest.md`**（cron，失败不覆盖上一份有效建议）。
从上往下读，顺序即决策优先级——**先卖后买**：

| 段落 | 回答什么 | 什么时候要动手 |
|---|---|---|
| ① 今日风险状态 | 总仓位上限（🟢=100% / 🔴=70%） | 转 🔴 当天把仓位降到上限内 |
| ② 结构灯 | 科创/题材加仓限制 | 科创50 🔴 时：暂停科创加仓、新增减半 |
| ③ 持仓健康度 | 每只持仓的止损线/浮亏/排雷 | 出现"已破❗"或引擎 REDUCE/DROP → 当天处置 |
| ④ 结构修正跟踪 | 离目标结构差多少 | 按"待执行"清单逐步调（不急于一天） |
| ⑤ 回补观察 | 割掉的票何时能接回 | 三条件 3/3 ✅ 且冷静期满才考虑 |
| ⑥ 个股抄底灯 | 新钱买什么、现在是不是窗口 | 见下节执行纪律 |

同一天既有该卖的又有想买的：**先处理 ③ 的风险，再看 ⑥ 的机会**。

---

## 三盏灯速查

### 主灯（沪深300）——总闸门
- 规则：昨收 < MA200 或 隔夜标普 ≤ -1.5%，连续 2 日确认 → 🔴，仓位上限降到 70%；反向同理转回 🟢。
- 回测：2024-01 崩盘段把 -12.6% 回撤压到 -3.9%。
- 单日破位不触发——晨报会显示"防守信号第 N 天"倒计时，提前想好减仓动作。

### 结构灯（科创50 / 中证1000）——风格闸门
- 科创50 🔴 = 科创/题材持仓暂停加仓、新增仓位减半（过了回测验收线，可执行）。
- 中证1000 仅展示状态（未过验收线，不给仓位指令），但它是**抄底灯的窗口开关**（见下）。

### 个股抄底灯——买点信号（只管新钱，不管持仓）

三态：

```
⚪ 未到 setup   距 120 日最高收盘 > -35%          → 与我无关
🟡 底部区观察   ≤ -35%（或近 60 日内曾达到）       → 进观察，不动手
🟢 可入场       🟡 且 L6 或 L1 触发（3 日展示窗口）
```

两个信号（只有这两个过了回测线，其余候选全部证伪）：
- **L6 超卖修复**（首选）：RSI14 连续 ≥3 天 ≤30 后，第一次回升穿越 30——"刀落地弹起来才伸手"。
- **L1 接刀**（次选）：setup 内 RSI14 首次跌破 30——买在恐慌最浓时，更便宜但打脸率更高。

**窗口规则（核心，与直觉相反）**：
- 中证1000 🔴（泥沙俱下）→ 左侧超额最大的窗口，可按纪律执行；
- 中证1000 🟢 而个股独自深跌 → 大概率自身有雷，**仅记录、不入场**（晨报会自动标注）。

**执行纪律（一条都不能省）**：
| 项 | 规则 |
|---|---|
| 成交 | 次日开盘价；开盘触涨跌停/一字不追 |
| 仓位 | 单票**半仓 ≤5% 总资产**（左侧止损宽，用仓位换空间） |
| 止损 | 入场价 − 3×ATR14（约 -10~-14%），此后按"入场以来最高收盘 − 2×ATR"只升不降 |
| 时限 | 60 个交易日没走出来 → 强制重评离场 |
| 并发 | 同时新开的抄底仓 ≤3 只（红灯期信号成簇 = 同一个赌注） |
| 冷却 | 同一只票平仓后 10 个交易日内不再进 |
| 红线 | **持仓票触发信号 ≠ 补仓理由**；绿灯期独跌票不碰；不摊平 |

预期管理：单笔期望 ≈ +2~5%、胜率 ≈ 55-60%（中性池口径，含成本），
不是每笔都赢——赢在纪律执行了足够多笔之后的期望值。

---

## 持仓纪律（③ 段的 SOP）

每只 HELD 有两条止损线，取严者：
- **硬止损** = 摊薄成本 × 0.92（-8% 兜底）
- **ATR 追踪线** = 近 20 日最高收盘 − 2×ATR14（随新高上移锁盈）

**破线当天的动作**：
1. 减/清该票（跌停卖不出就次日继续，别等反弹）；
2. `watchlist.yaml` 里把该票 state 改 `EXITED`，**必须加 `exit_date` 和 `exit_price`**（别删条目）：
   ```yaml
   - { symbol: "002842", name: "翔鹭钨业", state: "EXITED", exit_date: "2026-07-20", exit_price: 32.04 }
   ```
3. 之后晨报【回补观察】自动跟踪接回三条件（结构灯🟢 + 站回趋势线 + 引擎无负面）+ 10 日冷静期。

---

## watchlist.yaml 维护

| 场景 | 动作 |
|---|---|
| 看好一只新票 | watchlist 加一行 `state: "WATCHING"` → 抄底灯次日自动接管 |
| 按灯买入了 | 该票改 `state: "HELD"`，填 `position` 和 `entry_price`（摊薄成本） |
| 止损/清仓 | 改 `EXITED` + `exit_date`/`exit_price`（见上节） |
| 任何资金进出 | 更新顶层 `account.cash` 和 `as_of`（结构修正跟踪靠它算总资产） |
| 买了 ETF | 记入 `account.etf` 列表（引擎不扫 ETF，仅结构跟踪用） |

---

## 定时任务与文件地图

| 时间 | 任务 | 产出 |
|---|---|---|
| 工作日 09:00 | `scripts/morning_brief.sh` | `results/brief/latest.md` + 三份信号流水 |
| 每日 18:30 | 旧引擎 `daily_run.sh` | 排雷信号 + `cache/kline/` K线缓存（晨报的数据源！别停） |
| 周五 19:30 | `scripts/weekly_data.sh` | qlib 数据包 + 资金流 + 质检 |

三份信号流水（已进 git，是实盘对账的原始证据，**别删**）：
- `results/brief/signal_log.csv` — 主灯逐日状态
- `results/brief/lights_log.csv` — 结构灯逐日状态
- `results/brief/stock_lamps_log.csv` — 抄底灯每次 🟢 触发（按信号日去重）

## 手动命令速查

```bash
# 晨报没生成/想盘中重看（重跑同日自动去重）
.venv-quant/bin/python -m quant.live.daily_brief --out-dir results/brief

# 看 watchlist 全部票的灯态细节（含未进晨报的研究信号）
.venv-quant/bin/python -m quant.research.stock_lights --status

# 改灯参数前必跑对应回测（纪律：无回测不改参）
.venv-quant/bin/python -m quant.research.timing_lights    # 结构灯
.venv-quant/bin/python -m quant.research.stock_lights     # 个股抄底灯

# 测试
.venv-quant/bin/python -m pytest tests/quant/ -q
```

---

## 预期与局限（诚实清单）

- 回测超额在**衰减**：抄底灯交易层 2023-26 只剩约 +2.3%/笔（2019-22 为 +4.6%）。流水攒 2-3 个月后做实盘对账，用真数据校准预期。
- 自选池回测数字（胜率 83% 之类）含幸存者偏差——**预期一律用中性池口径**（+4.9% ex20 / 胜率 ~60%）。
- 红灯期信号一起亮 = 同一个"超跌反弹"赌注，并发上限是纪律不是建议。
- 灯只保证"期望为正的时机"，不保证个股不爆雷——名单质量靠你把关（这正是下一步"优质股筛选"要补的）。

## 什么时候找 Claude

- 改任何灯参数（先跑回测再改，报告落 `docs/quant/`）
- 月度/季度对账（流水 → 实际表现 vs 回测预期）
- 扩充/清理 watchlist（优质股筛选器，规划中）
- 加功能、修 bug、以及任何"这个数字为什么是这样"

---
*2026-07-19 建档（抄底灯上线同日）。改动本手册时同步更新 CLAUDE.md 的引用。*
